#include <bits/stdc++.h>
#include <stdio.h>
using namespace std;
int main()
{
	char a[100];
	scanf("%s",a);
	char cap[100],small[100];
	int s=0,c=0,count=0;
	for(int i=0;a[i]!='\0';i++)
	{
		count++;
		int q=(int)a[i];
		if(q<=90 && q>=65)
		{
			cap[c++]=a[i];
		}
		if(q<=122 && q>=97)
			small[s++]=a[i];
	}	
	for(int i=0i<25;i++)
	{
		char a='a';
		char A='A';
		if()
	}
}
for(i=0;i<25;i++)
	{
		if(==num)
		{
			found++;
			pos=i+1;
		}	
	}