#include <bits/stdc++.h>
using namespace std;
int main() 
{
    int t;
    cin>>t;
    while(t--)
    {
        int q;
        cin>>q;
        int a[q]={0};
        set<int> s;
        set<int> :: iterator it;
        for(int i=0;i<n;i++)
        {
            cin>>a[i];
            s.insert(a[i]);
        }
        int len=s.size();
        int a[len];
        int x=0;
        for(it=s.begin();it!=s.end();it++)
            a[x++]=
    }
	return 0;
	
}